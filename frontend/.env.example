# Copy to .env.local for local development, or set this same variable in
# your hosting provider's dashboard (e.g. Vercel) for production.
# Must point at your backend's /api path, with no trailing slash.
VITE_API_URL=http://localhost:4000/api
